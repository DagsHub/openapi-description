type: array
items:
  type: object
  properties:
    name:
      type: string
      description: Name/path of the bucket
    protocol:
      type: string
      enum: [s3, gs]
      description: Protocol used by the integration
    list_path:
      type: string
      description: URL to list the root of this bucket

