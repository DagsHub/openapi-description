type: array
items:
  - type: object
    properties:
      name:
        $ref: "../parameters/StorageBucket.yaml"
      protocol:
        $ref: "../parameters/StorageProtocol.yaml"
      list_path:
        type: string
        description: URL to list the root of this bucket

