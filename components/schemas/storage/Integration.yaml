type: array
items:
  type: object
  properties:
    name:
      type: string
      description: Name/path of the bucket
    protocol:
      type: string
      description: Protocol used by the integration (e.g. s3, gs, azure, http)
    list_path:
      type: string
      description: URL to list the root of this bucket
