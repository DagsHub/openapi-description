# This is a separate entity because we're using different examples for storage integrations
name: owner
example: DagsHub-Datasets
in: path
description: owner of the repository
required: true
schema:
  type: string