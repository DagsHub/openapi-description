type: object
properties:
  id:
    type: integer
  type:
    type: string
  config:
    $ref: './WebhookConfig.yaml'
  events:
    type: array
    items:
      type: string
  active:
    type: boolean
  updated_at:
    type: string
    format: date-time
  created_at:
    type: string
    format: date-time
