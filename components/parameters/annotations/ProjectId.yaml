name: project_id
in: query
required: false
description: Label Studio project ID to filter by. If not specified, returns access for all projects. Use -1 for wildcard access.
schema:
  type: integer
  format: int64
